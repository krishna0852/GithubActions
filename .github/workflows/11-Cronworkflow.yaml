name: Trigger workflow on Cron

on:
  schedule:
    #- cron: '* * * * *'

jobs:
  TriggerWorkflow:
    runs-on: ubuntu-latest
    steps:
      - name: Display Message
        run: echo "Workflow triggered by cron schedule"

           
      
